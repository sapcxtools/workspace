import { Component } from '@angular/core';

/**
 * Empty widget used to create space between other widgets on the dashboard.
 * The widget frame (drag, resize, visibility) is provided by cxdev-widget.
 */
@Component({
	selector: 'cxdev-dashboard-spacer',
	template: '',
})
export class CxdevDashboardSpacerComponent {}
