# CxdevConfigurations

In diesem Module werden alle projektbezogenen Konfigurationen provided. Die Konfiguration wird dabei in eine eigene Datei ausgelagert und anschließend im Module importiert. Konfigurationen werden nicht mehr nur als reine Konfigurationsdateien provided sondern als eine Factory Function. Als Beispiel kann die cxdev-layout.config.ts dienen.