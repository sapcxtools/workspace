<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project name="mybackoffice_buildcallbacks">
    <property name="mybackofficeWebFragmentSrc" value="${ext.mybackoffice.path}/resources/web-fragment.xml"/>
    <property name="mybackofficeWebFragmentDestJar" value="${ext.backoffice.path}/web/webroot/WEB-INF/lib/webfragmentExt_mybackoffice.jar"/>
	
    <macrodef name="mybackoffice_after_clean">
        <sequential>
            <mybackoffice_remove_web_fragment/>
        </sequential>
    </macrodef>
    	
    <macrodef name="mybackoffice_after_build">
        <sequential>
            <mybackoffice_remove_web_fragment/>
            <mybackoffice_create_web_fragment/>
        </sequential>
    </macrodef>
    	
    <macrodef name="mybackoffice_remove_web_fragment">
        <sequential>
            <if>
                <available file="${mybackofficeWebFragmentDestJar}"/>
                <then>
                    <delete file="${mybackofficeWebFragmentDestJar}"/>
                </then>
            </if>
        </sequential>
    </macrodef>
    	
    <macrodef name="mybackoffice_create_web_fragment">
        <sequential>
            <if>
                <available file="${mybackofficeWebFragmentSrc}"/>
                <then>
                    <sequential>
                        <jar destfile="${mybackofficeWebFragmentDestJar}">
                            <metainf file="${mybackofficeWebFragmentSrc}"/>
                        </jar>
                    </sequential>
                </then>
            </if>
        </sequential>
    </macrodef>
    	
    <patternset id="mybackoffice.localization.pattern">
        <!-- core and hmc  -->
        <include name="**/*locales*_en.properties"/>
        <exclude name="**/hmc/web/webroot/WEB-INF/classes/**/*"/>
        <exclude name="**/unittest/**/*"/>

        <!-- cockpit (ng) -->
        <include name="**/i3-label_en.properties"/>
        <include name="**/labels_en.properties"/>
        		
        <!-- messages -->
        <include name="**/*messages_en.properties"/>
        <include name="**/*messages.properties"/>
        		
        <!-- frontend properties files in WEB-INF/messages folder -->
        <include name="**/WEB-INF/messages/*_en.properties"/>
        		
        <!-- csv and impex -->
        <include name="**/*_en.csv"/>
        <include name="**/*_en.impex"/>
        <include name="**/lang-en.properties"/>
        		
        <!-- email localizations from yacceleratorcore -->
        <include name="resources/**/messages/*_en.properties"/>
        		
        <!-- include validation messages -->
        <include name="resources/**/ValidationMessages.properties"/>
    </patternset>
    	
    <macrodef name="mybackoffice_before_build">
		<sequential>
			<register_sass_extension extensionname="mybackoffice"/>
			<register_sass_extension extensionname="mybackoffice" resources="resources" destination=""/>
		</sequential>
	</macrodef>
</project>
