<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project name="cxbackoffice_buildcallbacks">
    <property name="cxbackofficeWebFragmentSrc" value="${ext.cxbackoffice.path}/resources/web-fragment.xml"/>
    <property name="cxbackofficeWebFragmentDestJar" value="${ext.backoffice.path}/web/webroot/WEB-INF/lib/webfragmentExt_cxbackoffice.jar"/>
	
    <macrodef name="cxbackoffice_after_clean">
        <sequential>
            <cxbackoffice_remove_web_fragment/>
        </sequential>
    </macrodef>
    	
    <macrodef name="cxbackoffice_after_build">
        <sequential>
            <cxbackoffice_remove_web_fragment/>
            <cxbackoffice_create_web_fragment/>
        </sequential>
    </macrodef>
    	
    <macrodef name="cxbackoffice_remove_web_fragment">
        <sequential>
            <if>
                <available file="${cxbackofficeWebFragmentDestJar}"/>
                <then>
                    <delete file="${cxbackofficeWebFragmentDestJar}"/>
                </then>
            </if>
        </sequential>
    </macrodef>
    	
    <macrodef name="cxbackoffice_create_web_fragment">
        <sequential>
            <if>
                <available file="${cxbackofficeWebFragmentSrc}"/>
                <then>
                    <sequential>
                        <jar destfile="${cxbackofficeWebFragmentDestJar}">
                            <metainf file="${cxbackofficeWebFragmentSrc}"/>
                        </jar>
                    </sequential>
                </then>
            </if>
        </sequential>
    </macrodef>
    	
    <patternset id="cxbackoffice.localization.pattern">
        <!-- core and hmc  -->
        <include name="**/*locales*_en.properties"/>
        <exclude name="**/hmc/web/webroot/WEB-INF/classes/**/*"/>
        <exclude name="**/unittest/**/*"/>

        <!-- cockpit (ng) -->
        <include name="**/i3-label_en.properties"/>
        <include name="**/labels_en.properties"/>
        		
        <!-- messages -->
        <include name="**/*messages_en.properties"/>
        <include name="**/*messages.properties"/>
        		
        <!-- frontend properties files in WEB-INF/messages folder -->
        <include name="**/WEB-INF/messages/*_en.properties"/>
        		
        <!-- csv and impex -->
        <include name="**/*_en.csv"/>
        <include name="**/*_en.impex"/>
        <include name="**/lang-en.properties"/>
        		
        <!-- email localizations from yacceleratorcore -->
        <include name="resources/**/messages/*_en.properties"/>
        		
        <!-- include validation messages -->
        <include name="resources/**/ValidationMessages.properties"/>
    </patternset>
    	
    <macrodef name="cxbackoffice_before_build">
		<sequential>
			<register_sass_extension extensionname="cxbackoffice"/>
			<register_sass_extension extensionname="cxbackoffice" resources="resources" destination=""/>
		</sequential>
	</macrodef>
</project>
