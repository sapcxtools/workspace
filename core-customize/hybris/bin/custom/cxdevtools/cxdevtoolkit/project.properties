# ----------------------------------------------------------------------------
# Copyright (c) 2026 CX DEV Tools
# ----------------------------------------------------------------------------

# Location of the file for the global platform spring application context
cxdevtoolkit.application-context=cxdevtoolkit-spring.xml

##############################################################################
# ImpEx global import configuration
##############################################################################
cxdevtoolkit.impeximport.configuration.legacymode=false
cxdevtoolkit.impeximport.configuration.enablecodeexecution=true
cxdevtoolkit.impeximport.configuration.validationmode=strict
cxdevtoolkit.impeximport.configuration.defaultlocale=en

##############################################################################
# Environment specific flags
#
# legacysystemsetup - Uses the legacy system setup, ie. running the impex from this extension
# configurationfile - Absolute filename for the persistent configuration of
#                     the system setup environment, defaults to
#                     ${HYBRIS_DATA_DIR}/cxdevtoolkit/configuration.properties
# isdevelopment     - Should be set on local development environment.
#                     If set to true, sample and test data is imported
#                     automatically during system initialization
# supportlocalizedfiles - If set to true, import of a file will also trigger
#                     an import of the file with pattern <file>_isocode.<ext>
#                     for all available languages within the system
# importsampledata  - If set to true, sample data is imported automatically
#                     during system initialization or system update
# importtestdata    - If set to true, test data is imported automatically
#                     during system initialization or system update
##############################################################################
cxdevtoolkit.impeximport.environment.legacysystemsetup=true
cxdevtoolkit.impeximport.environment.configurationfile=${HYBRIS_DATA_DIR}/cxdevtoolkit/configuration.properties
cxdevtoolkit.impeximport.environment.isdevelopment=false
cxdevtoolkit.impeximport.environment.supportlocalizedfiles=false
cxdevtoolkit.impeximport.environment.importsampledata=false
cxdevtoolkit.impeximport.environment.importtestdata=false

##############################################################################
# ImpEx import filesets
#
# Import files can be specified in any property file that is considered as a
# CX DEV configuration location, e.g.:
# - properties area of manifest.json file
# - global local.properties file
# - extension specific project.properties file
#
# The pattern needs to apply to the following rule:
# cxdevtoolkit.impeximport.<type>.[<version>.]<order>.<name>
#
# - <type>     must be one of:
#              elementarydata - only imported during initialization
#              releasepatch   - only imported once per system during update
#              essentialdata  - always imported during initialization or update
#              projectdata    - only imported if configured or selected manually
# - <version>  only required for type releasepatch!
#              It must identify the release version, that should be alphanumeric
#              In other words you must guarantee that the order or the release
#              versions is correct, as the mechanism relies on alphanumerical order
# - <order>    optional: additional level for alphanumerical ordering
# - <name>     identifier for the import (can be any unique number or text)
#
# Examples:
# cxdevtoolkit.impeximport.elementarydata.0100.coredata=/path/to/file.impex
# cxdevtoolkit.impeximport.elementarydata.0500.catalogs=/path/to/file.impex
# cxdevtoolkit.impeximport.releasepatch.release1x0x0.0001.datamigration=/path/to/file.impex
# cxdevtoolkit.impeximport.releasepatch.release1x1x0.0001.datamigration=/path/to/file.impex
# cxdevtoolkit.impeximport.releasepatch.release2x0x0.0001.insertdefaultvalue=/path/to/file.impex
# cxdevtoolkit.impeximport.essentialdata.0010.userrights=/path/to/file.impex
# cxdevtoolkit.impeximport.essentialdata.0300.solrconfiguration=/path/to/file.impex
# cxdevtoolkit.impeximport.essentialdata.5000.cmstemplates=/path/to/file.impex
# cxdevtoolkit.impeximport.overlay.1000.core-essential=/path/to/file.impex
# cxdevtoolkit.impeximport.sampledata.0100.categories=/path/to/file.impex
# cxdevtoolkit.impeximport.sampledata.0200.classificationsystem=/path/to/file.impex
# cxdevtoolkit.impeximport.sampledata.0500.products=/path/to/file.impex
# cxdevtoolkit.impeximport.sampledata.1000.users=/path/to/file.impex
# cxdevtoolkit.impeximport.sampledata.5000.cms=/path/to/file.impex
# cxdevtoolkit.impeximport.testdata.0100.categories=/path/to/file.impex
# cxdevtoolkit.impeximport.testdata.0500.products=/path/to/file.impex
# cxdevtoolkit.impeximport.testdata.1000.users=/path/to/file.impex
# cxdevtoolkit.impeximport.testdata.5000.cms=/path/to/file.impex
#
#
# For full support, one should activate the project data of this extension, e.g.
# update.executeProjectData.extensionName.list=cxdevtoolkit
##############################################################################

##############################################################################
# Html Email Service Fake configuration
#
# In order to activate the fake, you need to add a spring profile to your configuration:
# spring.profiles.active=cxdevtoolkit-fake-localmails
#
# - <method>            the method to store mails locally, either file or database
# - <directory>         the directory to which the email files will be stored to
# - <extension>         the extension for the email files, defaults to eml
# - <filenamepattern>   the pattern for the generated files. It can be adjusted with the following parameters:
#                       - timestamp: the timestamp of the moment of sending
#                       - datetime:  the date/time of the moment of sending (format: YYYYMMdd-HHmmssS)
#                       - subject:   the subject of the email
#                       - from:      the senders address (at sign replaced with __at__)
#                       - to:        the (first) receivers address (at sign replaced with __at__)
#                       - extension: the extension as configured above
# - <mediafolder>       the media folder to place fake email media items into (default: fake-emails)
# - <daysToKeep>        the number of days to keep local emails in the database (default: 7)
#
# Note: Please make sure that you have set the following properties, otherwise the email generation process will fail:
# - mail.smtp.server (DEFAULT: MISSING!!!)
# - mail.from (DEFAULT: test@hybris.de)
##############################################################################
cxdevtoolkit.fakes.htmlEmailService.localstorage.method=file
cxdevtoolkit.fakes.htmlEmailService.localstorage.directory=${HYBRIS_LOG_DIR}/mails
cxdevtoolkit.fakes.htmlEmailService.localstorage.filenamepattern={timestamp}_{subject}.{extension}
cxdevtoolkit.fakes.htmlEmailService.localstorage.extension=eml
cxdevtoolkit.fakes.htmlEmailService.localstorage.mediafolder=fake-emails
cxdevtoolkit.fakes.htmlEmailService.localstorage.daysToKeep=7
